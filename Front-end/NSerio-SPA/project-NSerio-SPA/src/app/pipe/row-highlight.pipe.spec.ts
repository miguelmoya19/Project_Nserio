import { RowHighlightPipe } from './row-highlight.pipe';

describe('RowHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new RowHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
